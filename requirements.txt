# ─── Core Data Science & ML ──────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0
matplotlib>=3.7.0
seaborn>=0.12.0
pillow>=9.5.0
opencv-python>=4.7.0

# ─── Deep Learning (PyTorch) ──────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0          # Required by ConvNeXt, EfficientNet, Swin-T in factory.py

# ─── ONNX Edge Runtime ────────────────────────────────────────────────────────
onnxruntime>=1.16.0  # CPU inference (<3.5 ms latency)
onnx>=1.14.0         # For ONNX model export in training notebooks

# ─── Explainable AI (XAI) ─────────────────────────────────────────────────────
# shap>=0.41.0       # Optional — not used in core pipeline
# lime>=0.2.0        # Optional — not used in core pipeline

# ─── Web Backend API ──────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6  # Required by FastAPI for File() upload endpoints
requests>=2.28.0

# ─── Image Hashing (Leakage Audit) ────────────────────────────────────────────
imagehash>=4.3.1

# ─── Utilities & Configs ──────────────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.65.0
tabulate>=0.9.0
python-dotenv>=1.0.0
scipy>=1.10.0        # Used for Wilson score confidence intervals in Step 8E
